version: 4
project: bad-include
metered_prices:
  - id: api_calls
    name: "API calls"
    unit: "calls"
    unit_price: 0.001
    stripe_meter_id: mtr_xyz
    period: month
    currency: usd
    aggregation: sum
plans:
  - id: pro
    name: "Pro"
    includes:
      api_calls: 1000
      other_meter: 500
